# TaskFlow AI

A local-first static MVP for turning messy messages, bills, and reminders into organized actions.

## Run locally

Open `index.html` in a browser. The app stores demo tasks in browser local storage.

## Deploy

This folder is intentionally separate from `github-portfolio`. Create a dedicated GitHub repository for TaskFlow AI, then publish this folder with GitHub Pages or a static host such as Vercel.
